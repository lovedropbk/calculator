<Application
    x:Class="FinancialCalculator.WinUI3.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FinancialCalculator.WinUI3"
    xmlns:conv="using:FinancialCalculator.WinUI3.Converters"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:nf="using:Windows.Globalization.NumberFormatting"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
            
                <muxc:XamlControlsResources />
                <!-- Include the new style resource dictionaries -->
                <ResourceDictionary Source="Styles/DefaultStyles.xaml"/>
                <ResourceDictionary Source="Styles/ResponsiveStyles.xaml"/>
                <ResourceDictionary Source="Styles/FluentTheme.xaml"/>
                <ResourceDictionary Source="Styles/DesignerStyles.xaml"/>
                <!-- Density dictionary: default to Compact; can be swapped at runtime -->
                <ResourceDictionary Source="Styles/Density.Compact.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- MARK: Enhanced Theme Brushes with Animations -->
            <ResourceDictionary.ThemeDictionaries>
                <ResourceDictionary x:Key="Light">
                    <SolidColorBrush x:Key="PillBackgroundBrush" Color="#F5F5F5" />
                    <SolidColorBrush x:Key="PillStrokeBrush" Color="#D0D0D0" />
                    <SolidColorBrush x:Key="HighlightBrush" Color="#0078D4" Opacity="0.1" />
                    <SolidColorBrush x:Key="SuccessBrush" Color="#107C10" />
                    <SolidColorBrush x:Key="WarningBrush" Color="#FFB900" />
                    <SolidColorBrush x:Key="ErrorBrush" Color="#D83B01" />
                    <!-- Themed palette for budget utilization -->
                    <SolidColorBrush x:Key="BudgetCashBrush" Color="#0078D7"/>
                    <SolidColorBrush x:Key="BudgetSubDownBrush" Color="#0099BC"/>
                    <SolidColorBrush x:Key="BudgetRateSubsidyBrush" Color="#00B7C3"/>
                    <SolidColorBrush x:Key="BudgetIdcBrush" Color="#038387"/>
                    <SolidColorBrush x:Key="BudgetUnallocatedBrush" Color="#737373"/>
                </ResourceDictionary>
                <ResourceDictionary x:Key="Dark">
                    <SolidColorBrush x:Key="PillBackgroundBrush" Color="#303030" />
                    <SolidColorBrush x:Key="PillStrokeBrush" Color="#505050" />
                    <SolidColorBrush x:Key="HighlightBrush" Color="#0078D4" Opacity="0.2" />
                    <SolidColorBrush x:Key="SuccessBrush" Color="#10A010" />
                    <SolidColorBrush x:Key="WarningBrush" Color="#FFD700" />
                    <SolidColorBrush x:Key="ErrorBrush" Color="#FF6B6B" />
                    <!-- Themed palette for budget utilization -->
                    <SolidColorBrush x:Key="BudgetCashBrush" Color="#0078D7"/>
                    <SolidColorBrush x:Key="BudgetSubDownBrush" Color="#0099BC"/>
                    <SolidColorBrush x:Key="BudgetRateSubsidyBrush" Color="#00B7C3"/>
                    <SolidColorBrush x:Key="BudgetIdcBrush" Color="#038387"/>
                    <SolidColorBrush x:Key="BudgetUnallocatedBrush" Color="#737373"/>
                </ResourceDictionary>
                <ResourceDictionary x:Key="HighContrast">
                    <!-- High Contrast overrides for custom brushes -->
                    <SolidColorBrush x:Key="PillBackgroundBrush" Color="#00000000" />
                    <SolidColorBrush x:Key="PillStrokeBrush" Color="#000000" />

                    <!-- Budget palette in High Contrast: map to system colors for visibility -->
                    <SolidColorBrush x:Key="BudgetCashBrush" Color="{ThemeResource SystemColorHighlightColor}"/>
                    <SolidColorBrush x:Key="BudgetSubDownBrush" Color="{ThemeResource SystemColorWindowTextColor}"/>
                    <SolidColorBrush x:Key="BudgetRateSubsidyBrush" Color="{ThemeResource SystemColorHotlightColor}"/>
                    <SolidColorBrush x:Key="BudgetIdcBrush" Color="{ThemeResource SystemColorGrayTextColor}"/>
                    <SolidColorBrush x:Key="BudgetUnallocatedBrush" Color="{ThemeResource SystemColorWindowFrameColor}"/>
                </ResourceDictionary>
            </ResourceDictionary.ThemeDictionaries>

            <!-- Converters -->
            <conv:StringEqualsToVisibilityConverter x:Key="StringEqualsToVisibilityConverter" />
            <conv:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
            <conv:NullToBoolConverter x:Key="NullToBoolConverter" />
            <conv:BoolToThicknessConverter x:Key="BoolToThicknessConverter" />
            <conv:BoolToGridLengthConverter x:Key="BoolToGridLengthConverter" />
            <conv:BoolToDoubleConverter x:Key="BoolToDoubleConverter" />
            <conv:BoolNotConverter x:Key="BoolNotConverter" />
            <conv:TextFormatConverter x:Key="TextFormatConverter" />

            <!-- MARK: Enhanced Card Container Style with Shadow -->
            <Style x:Key="CardContainerStyle" TargetType="Border">
                <Setter Property="Background" Value="{ThemeResource CardBackgroundFillColorDefaultBrush}" />
                <Setter Property="BorderBrush" Value="{ThemeResource ControlStrokeColorDefaultBrush}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="CornerRadius" Value="8" />
                <Setter Property="Padding" Value="16" />
                <!-- Translation cannot be set in a Setter as it's not a DependencyProperty -->
                <Setter Property="Shadow">
                    <Setter.Value>
                        <ThemeShadow />
                    </Setter.Value>
                </Setter>
                <Setter Property="Transitions">
                    <Setter.Value>
                        <TransitionCollection>
                            <EntranceThemeTransition/>
                        </TransitionCollection>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- MARK: Pill Style (for IDC auto/override indicator) -->
            <Style x:Key="PillBorderStyle" TargetType="Border">
                <Setter Property="Background" Value="{ThemeResource PillBackgroundBrush}" />
                <Setter Property="BorderBrush" Value="{ThemeResource PillStrokeBrush}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="CornerRadius" Value="16" />
                <Setter Property="Padding" Value="6,2" />
                <Setter Property="Margin" Value="4,0,0,0" />
                <Setter Property="VerticalAlignment" Value="Center" />
            </Style>

            <!-- MARK: Enhanced Input Defaults with Hover Effects -->
            <!-- Input density is centralized in Styles/FluentTheme.xaml via Dense* keyed styles (DenseTextBoxStyle, DenseTextBoxWithSuffixStyle, DenseComboBoxStyle, DenseNumberBoxStyle). Legacy Compact* styles have been removed to avoid drift. See Fluent tokens and styles in [Styles/FluentTheme.xaml](winui3-mvp/FinancialCalculator.WinUI3/Styles/FluentTheme.xaml). -->

            <!-- Global Button style removed to preserve Fluent defaults.
                 Use PrimaryActionButtonStyle / SecondaryButtonStyle / IconButtonStyle explicitly. -->

            <!-- Enhanced Button Hover Style -->
            <Style x:Key="HoverButtonStyle" TargetType="Button">
                <Setter Property="CornerRadius" Value="6" />
                <Setter Property="Margin" Value="6,0,0,0" />
                <Setter Property="Padding" Value="10,6,10,6" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="MinWidth" Value="80" />
                <Setter Property="Background" Value="{ThemeResource ButtonBackground}" />
                <Setter Property="Transitions">
                    <Setter.Value>
                        <TransitionCollection>
                            <RepositionThemeTransition />
                        </TransitionCollection>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Numeric formatters -->
            <nf:DecimalFormatter x:Key="GroupedZeroFormatter" IsGrouped="True" FractionDigits="0" />
            <nf:DecimalFormatter x:Key="GroupedTwoFormatter" IsGrouped="True" FractionDigits="2" />

            <!-- Shared widths for consistent alignment -->
            <GridLength x:Key="UnitColumnWidthValue">70</GridLength>
            <GridLength x:Key="InputColumnWidthValue">260</GridLength>
            <x:Double x:Key="ValueAreaWidth">320</x:Double>
            <x:Double x:Key="LabelColumnWidthValue">160</x:Double>
            <!-- Details panel fixed column widths to prevent value shifting -->
            <GridLength x:Key="DetailsLabelColumnWidth">160</GridLength>
            <GridLength x:Key="DetailsValueColumnWidth">360</GridLength>
            <x:Double x:Key="DealInputsCardMaxWidth">520</x:Double>

            <!-- Unified unit suffix helpers -->
            <Thickness x:Key="PercentSuffixMargin">0,0,4,0</Thickness>
            <Thickness x:Key="CurrencySuffixMargin">0,0,20,0</Thickness>
            <Style x:Key="UnitSuffixTextStyle" TargetType="TextBlock">
                <Setter Property="HorizontalAlignment" Value="Right"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="Opacity" Value="0.6"/>
                <Setter Property="IsHitTestVisible" Value="False"/>
            </Style>
            <!-- Align expanders with value column (LabelColumn 160 + 8px gap) -->
            <Thickness x:Key="ValueAreaOffsetMargin">168,12,0,0</Thickness>

            <!-- MARK: Typography helpers -->
            <!-- CardHeaderTextStyle moved to Styles/DefaultStyles.xaml -->
            <Style x:Key="SectionHeaderTextStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="16" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="Margin" Value="0,0,8,0" />
                <Setter Property="VerticalAlignment" Value="Center" />
            </Style>
            <!-- LabelTextStyle moved to Styles/DefaultStyles.xaml -->

            <!-- Enhanced Validation/Error text style with Icon -->
            <Style x:Key="ErrorTextStyle" TargetType="TextBlock">
                <Setter Property="Foreground" Value="{ThemeResource ErrorBrush}" />
                <Setter Property="Margin" Value="0,8,0,0" />
                <Setter Property="TextWrapping" Value="Wrap" />
                <Setter Property="FontSize" Value="13" />
            </Style>

            <!-- Success text style -->
            <Style x:Key="SuccessTextStyle" TargetType="TextBlock">
                <Setter Property="Foreground" Value="{ThemeResource SuccessBrush}" />
                <Setter Property="Margin" Value="0,8,0,0" />
                <Setter Property="TextWrapping" Value="Wrap" />
                <Setter Property="FontSize" Value="13" />
            </Style>

            <!-- Warning text style -->
            <Style x:Key="WarningTextStyle" TargetType="TextBlock">
                <Setter Property="Foreground" Value="{ThemeResource WarningBrush}" />
                <Setter Property="Margin" Value="0,8,0,0" />
                <Setter Property="TextWrapping" Value="Wrap" />
                <Setter Property="FontSize" Value="13" />
            </Style>

            <!-- Loading Indicator Template -->
            <DataTemplate x:Key="LoadingTemplate">
                <Grid Background="{ThemeResource LayerFillColorDefaultBrush}">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="{StaticResource SpaceM}">
                        <ProgressRing IsActive="True" Width="{StaticResource LoaderIndicatorSize}" Height="{StaticResource LoaderIndicatorSize}" />
                        <TextBlock Text="Calculating..." HorizontalAlignment="Center" />
                    </StackPanel>
                </Grid>
            </DataTemplate>
        </ResourceDictionary>
    </Application.Resources>
</Application>
