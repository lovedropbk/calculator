<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls">

    <!-- ========================================== -->
    <!-- THEME COLORS & BRUSHES                    -->
    <!-- ========================================== -->
    
    <!-- Accent Colors for Financial Calculator -->
    <SolidColorBrush x:Key="PrimaryAccentColorBrush" Color="#0078D4"/>
    <SolidColorBrush x:Key="SecondaryAccentColorBrush" Color="#005A9E"/>
    <SolidColorBrush x:Key="SuccessColorBrush" Color="#107C10"/>
    <SolidColorBrush x:Key="WarningColorBrush" Color="#FFB900"/>
    <SolidColorBrush x:Key="ErrorColorBrush" Color="#D83B01"/>
    <SolidColorBrush x:Key="InfoColorBrush" Color="#0078D4"/>
    
    <!-- Gradient Brushes for Modern Look -->
    <LinearGradientBrush x:Key="HeaderGradientBrush" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#0078D4" Offset="0"/>
        <GradientStop Color="#005A9E" Offset="1"/>
    </LinearGradientBrush>

    <!-- ========================================== -->
    <!-- ANIMATIONS & TRANSITIONS                  -->
    <!-- ========================================== -->
    
    <Storyboard x:Key="FadeInStoryboard">
        <DoubleAnimation Storyboard.TargetProperty="Opacity" 
                         From="0" To="1" Duration="0:0:0.3">
            <DoubleAnimation.EasingFunction>
                <QuadraticEase EasingMode="EaseOut"/>
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
    </Storyboard>

    <Storyboard x:Key="PulseAnimation">
        <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="Opacity" 
                                        RepeatBehavior="Forever">
            <LinearDoubleKeyFrame KeyTime="0:0:0" Value="1"/>
            <LinearDoubleKeyFrame KeyTime="0:0:0.5" Value="0.5"/>
            <LinearDoubleKeyFrame KeyTime="0:0:1" Value="1"/>
        </DoubleAnimationUsingKeyFrames>
    </Storyboard>

    <!-- ========================================== -->
    <!-- ENHANCED BUTTON STYLES                    -->
    <!-- ========================================== -->
    
    <!-- Primary Action Button with Visual States -->
    <Style x:Key="PrimaryActionButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="{ThemeResource AccentButtonBackground}"/>
        <Setter Property="Foreground" Value="{ThemeResource AccentButtonForeground}"/>
        <Setter Property="BorderBrush" Value="{ThemeResource AccentButtonBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="12,6,12,6"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="CornerRadius" Value="6"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid x:Name="RootGrid">
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="RootGrid" 
                                                         Storyboard.TargetProperty="Opacity"
                                                         To="1" Duration="0:0:0.1"/>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="PointerOver">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="BackgroundBorder" 
                                                         Storyboard.TargetProperty="Opacity"
                                                         To="0.9" Duration="0:0:0.1"/>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter"
                                                                        Storyboard.TargetProperty="Foreground">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource AccentButtonForegroundPointerOver}"/>
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Pressed">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="RootGrid" 
                                                         Storyboard.TargetProperty="Opacity"
                                                         To="0.8" Duration="0:0:0.1"/>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Disabled">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="RootGrid" 
                                                         Storyboard.TargetProperty="Opacity"
                                                         To="0.4" Duration="0:0:0.1"/>
                                    </Storyboard>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <Border x:Name="BackgroundBorder" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{TemplateBinding CornerRadius}">
                            <Border.Shadow>
                                <ThemeShadow />
                            </Border.Shadow>
                        </Border>
                        <ContentPresenter x:Name="ContentPresenter"
                                          Content="{TemplateBinding Content}"
                                          ContentTemplate="{TemplateBinding ContentTemplate}"
                                          Padding="{TemplateBinding Padding}"
                                          HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                                          AutomationProperties.AccessibilityView="Raw"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Secondary Button Style -->
    <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource DefaultButtonStyle}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="{ThemeResource ButtonBorderBrush}"/>
        <Setter Property="CornerRadius" Value="6"/>
        <Setter Property="Padding" Value="10,4,10,4"/>
    </Style>

    <!-- Copy Button Style with Success Feedback -->
    <Style x:Key="CopyButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="{ThemeResource ButtonBackground}"/>
        <Setter Property="Foreground" Value="{ThemeResource ButtonForeground}"/>
        <Setter Property="BorderBrush" Value="{ThemeResource ButtonBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8,2,8,2"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="CornerRadius" Value="4"/>
    </Style>

    <!-- ========================================== -->
    <!-- ENHANCED TEXTBOX STYLES                   -->
    <!-- ========================================== -->
    
    <Style x:Key="ModernTextBoxStyle" TargetType="TextBox">
        <Setter Property="CornerRadius" Value="6"/>
        <Setter Property="MinWidth" Value="96"/>
        <Setter Property="Margin" Value="4,0,0,0"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8,6,8,6"/>
        <Setter Property="PlaceholderForeground" Value="{ThemeResource TextControlPlaceholderForeground}"/>
    </Style>

    <!-- Error State TextBox -->
    <Style x:Key="ErrorTextBoxStyle" TargetType="TextBox" BasedOn="{StaticResource ModernTextBoxStyle}">
        <Setter Property="BorderBrush" Value="#D83B01"/>
        <Setter Property="BorderThickness" Value="2"/>
    </Style>

    <!-- ========================================== -->
    <!-- ENHANCED COMBOBOX STYLES                  -->
    <!-- ========================================== -->
    
    <Style x:Key="ModernComboBoxStyle" TargetType="ComboBox">
        <Setter Property="CornerRadius" Value="6"/>
        <Setter Property="MinWidth" Value="120"/>
        <Setter Property="Margin" Value="4,0,0,0"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8,6,32,6"/>
    </Style>

    <!-- ========================================== -->
    <!-- LOADING & PROGRESS INDICATORS             -->
    <!-- ========================================== -->
    
    <!-- Loading Overlay Style -->
    <Style x:Key="LoadingOverlayStyle" TargetType="Grid">
        <Setter Property="Background">
            <Setter.Value>
                <SolidColorBrush Color="Black" Opacity="0.3"/>
            </Setter.Value>
        </Setter>
        <Setter Property="Visibility" Value="Collapsed"/>
    </Style>

    <!-- Progress Ring Container -->
    <Style x:Key="ProgressContainerStyle" TargetType="Border">
        <Setter Property="Background" Value="{ThemeResource AcrylicBackgroundFillColorDefaultBrush}"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="Padding" Value="24"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeBrush}"/>
    </Style>

    <!-- ========================================== -->
    <!-- INFOBAR STYLES                            -->
    <!-- ========================================== -->
    
    <Style x:Key="NotificationInfoBarStyle" TargetType="muxc:InfoBar">
        <Setter Property="CornerRadius" Value="6"/>
        <Setter Property="Margin" Value="0,8,0,8"/>
        <Setter Property="IsIconVisible" Value="True"/>
        <Setter Property="IsClosable" Value="True"/>
    </Style>

    <!-- ========================================== -->
    <!-- TAB STYLES WITH ICONS                     -->
    <!-- ========================================== -->
    
    <Style x:Key="IconTabViewItemStyle" TargetType="TabViewItem">
        <Setter Property="MinHeight" Value="48"/>
        <Setter Property="Padding" Value="12,8,12,8"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>

    <!-- ========================================== -->
    <!-- DATAGRID ENHANCEMENTS                     -->
    <!-- ========================================== -->
    
    <Style x:Key="ModernDataGridStyle" TargetType="controls:DataGrid">
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeBrush}"/>
        <Setter Property="VerticalGridLinesBrush" Value="{ThemeResource SystemControlForegroundBaseLowBrush}"/>
        <Setter Property="HorizontalGridLinesBrush" Value="{ThemeResource SystemControlForegroundBaseLowBrush}"/>
        <Setter Property="RowBackground" Value="Transparent"/>
        <Setter Property="AlternatingRowBackground" Value="{ThemeResource CardBackgroundBrush}"/>
        <Setter Property="HeadersVisibility" Value="Column"/>
        <Setter Property="GridLinesVisibility" Value="All"/>
    </Style>

    <!-- ========================================== -->
    <!-- TOOLTIP STYLES                            -->
    <!-- ========================================== -->
    
    <Style x:Key="InformativeToolTipStyle" TargetType="ToolTip">
        <Setter Property="Background" Value="{ThemeResource AcrylicBackgroundFillColorDefaultBrush}"/>
        <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="12,8,12,8"/>
        <Setter Property="FontSize" Value="13"/>
    </Style>

    <!-- ========================================== -->
    <!-- CARD CONTAINER ENHANCED                   -->
    <!-- ========================================== -->
    
    <Style x:Key="EnhancedCardStyle" TargetType="Border">
        <Setter Property="Background" Value="{ThemeResource CardBackgroundBrush}"/>
        <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="Padding" Value="16"/>
        <Setter Property="Margin" Value="4"/>
    </Style>

    <!-- Card with Hover Effect -->
    <Style x:Key="InteractiveCardStyle" TargetType="Border" BasedOn="{StaticResource EnhancedCardStyle}">
        <Setter Property="Transitions">
            <Setter.Value>
                <TransitionCollection>
                    <EntranceThemeTransition/>
                </TransitionCollection>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ========================================== -->
    <!-- STATUS BAR STYLES                         -->
    <!-- ========================================== -->
    
    <Style x:Key="StatusTextStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Opacity" Value="0.8"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <Style x:Key="StatusSuccessTextStyle" TargetType="TextBlock" BasedOn="{StaticResource StatusTextStyle}">
        <Setter Property="Foreground">
            <Setter.Value>
                <SolidColorBrush Color="#107C10"/>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="StatusErrorTextStyle" TargetType="TextBlock" BasedOn="{StaticResource StatusTextStyle}">
        <Setter Property="Foreground">
            <Setter.Value>
                <SolidColorBrush Color="#D83B01"/>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ========================================== -->
    <!-- SCROLLVIEWER ENHANCEMENTS                 -->
    <!-- ========================================== -->
    
    <Style x:Key="ModernScrollViewerStyle" TargetType="ScrollViewer">
        <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
        <Setter Property="HorizontalScrollBarVisibility" Value="Auto"/>
        <Setter Property="ZoomMode" Value="Disabled"/>
        <Setter Property="Padding" Value="0,0,8,0"/>
    </Style>

    <!-- ========================================== -->
    <!-- RESPONSIVE LAYOUT HELPERS                 -->
    <!-- ========================================== -->
    
    <x:Double x:Key="CompactModeWidth">600</x:Double>
    <x:Double x:Key="MediumModeWidth">900</x:Double>
    <x:Double x:Key="WideModeWidth">1200</x:Double>
    
    <x:Double x:Key="MinCardWidth">320</x:Double>
    <x:Double x:Key="MaxCardWidth">600</x:Double>
    
    <GridLength x:Key="CompactColumnWidth">1*</GridLength>
    <GridLength x:Key="StandardColumnWidth">2*</GridLength>

</ResourceDictionary>
